# Copy to .env.local for local dev. Set the same keys in Vercel (Production + Preview).
# Never commit .env.local. Get values from Supabase: Project Settings → API.

NEXT_PUBLIC_SUPABASE_URL=https://rotolbancrxzgoizrjsl.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_GYDYL6Qs0BoiCRFCPxLTRg_U7-8nZQ8

# Server-only (Route Handlers). Optional until Phase 4 if unused.
SUPABASE_SERVICE_ROLE_KEY=

# Phase 3 — GitHub Contents API (server-only, never NEXT_PUBLIC_*).
GITHUB_TOKEN=
GITHUB_REPOSITORY=
# Defaults to main if omitted.
GITHUB_DEFAULT_BRANCH=
